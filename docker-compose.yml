version : '3'
services :
  server :
    env_file:
      - .env
    build: 
      context: .
    image: ghcr.io/skeletonbear/back-end-punmaew:latest
    ports:
      - "3000:3000"
networks:
  default:
    external:
      name: punmaew

